import PageHeader from "../components/ui/PageHeader";

export default function TasksPage() {
  return (
    <div>
      <PageHeader title="Tasks" description="Manage watering, fertilizing, and harvesting tasks." />
      <p>Build the tasks module here.</p>
    </div>
  );
}
