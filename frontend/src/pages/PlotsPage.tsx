import PageHeader from "../components/ui/PageHeader";

export default function PlotsPage() {
  return (
    <div>
      <PageHeader title="Plots" description="Manage plots or fields inside each farm." />
      <p>Build the plots module here.</p>
    </div>
  );
}
