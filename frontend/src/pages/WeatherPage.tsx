import PageHeader from "../components/ui/PageHeader";

export default function WeatherPage() {
  return (
    <div>
      <PageHeader title="Weather" description="View forecast data and sync weather updates." />
      <p>Build the weather module here.</p>
    </div>
  );
}
