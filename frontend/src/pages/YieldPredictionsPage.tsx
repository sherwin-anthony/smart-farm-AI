import PageHeader from "../components/ui/PageHeader";

export default function YieldPredictionsPage() {
  return (
    <div>
      <PageHeader title="Yield Predictions" description="Create and review crop yield estimates." />
      <p>Build the yield prediction module here.</p>
    </div>
  );
}
